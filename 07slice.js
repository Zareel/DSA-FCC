//* Copy Array Items Using slice()

//? We have defined a function, forecast, that takes an array as an argument. Modify the function using slice() to extract information from the argument array and return a new array that contains the string elements warm and sunny.

// slice() => extracts a given number of elements into a new array
//slice() => takes two parameters
// first parameter => determines from where the extaction bigins
// second parameter => indicates upto which element to be extacted not including the elemengt at this index

function forecast(arr) {
  // Only change code below this line

  return arr;
}

// Only change code above this line
console.log(
  forecast(["cold", "rainy", "warm", "sunny", "cool", "thunderstorms"])
);
